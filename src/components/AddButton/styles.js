export const styles = theme => ({
    button: {
        // position: 'absolute',
        // bottom: theme.spacing.unit * 4,
        // right: theme.spacing.unit * 4,

        margin: 0,
        top: 'auto',
        right: 35,
        bottom: 20,
        left: 'auto',
        position: 'fixed',
        zIndex: 1,
    },
});