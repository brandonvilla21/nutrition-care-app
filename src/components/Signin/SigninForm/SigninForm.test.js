// import React from 'react';
// import { shallow, mount } from 'enzyme';
// import SigninForm from './SigninForm';
// import TextField from '@material-ui/core/TextField';
// import Button from '@material-ui/core/Button';

it( 'Adapt the test to use react-testing-library' );


// describe('SigninForm component', () => {
//     it('renders SigninForm without crashing', () => {
//         const wrapper = shallow(<SigninForm />);
//     })
    
//     /**
//      * The 5 Fields are for:
//      * Name, Lastname, Email, Password and Password confirmation
//      */
//     it('renders 5 TextField components', () => {
//         const wrapper = shallow(<SigninForm />);
//         expect(wrapper.find(TextField)).toHaveLength(5);
//     })

//     it('renders a Button component', () => {
//         const wrapper = shallow(<SigninForm />);
//         expect(wrapper.find(Button)).toHaveLength(1);
//     })

//     it('renders a Button with variant extended', () => {
//         const wrapper = shallow(<SigninForm />);
//         const buttonComponent = wrapper.find(Button);
//         const variantProp = buttonComponent.props().variant;
//         expect(variantProp).toEqual('extendedFab');
//     })
    
// })
