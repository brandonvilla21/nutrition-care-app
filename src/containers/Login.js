import React from 'react';
import Layout from '../layouts/Session';

const Login = (props) => {
    return (
        <Layout>
            <h5>Login Content (Here goes the login content)</h5>
        </Layout>
    );
}

export default Login;